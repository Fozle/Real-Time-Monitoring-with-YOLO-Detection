# Networked YOLO Video Stream

A collaborative project demonstrating real-time object detection over a local network using Flask, OpenCV, and YOLOv8.

## 💻 Roles & Network Configuration
* **Laptop A (Sender):** — *Runs the Flask Server*
* **Laptop B (Receiver):**  — *Runs the YOLO Detection*
* **Sender IP Address:** `

---

## 🚀 Execution Process

### 1. Starting the Stream
To initiate the video feed, we executed the following on **Laptop A**:
```bash
python app.py# Real-Time-Monitoring-with-YOLO-Detection
